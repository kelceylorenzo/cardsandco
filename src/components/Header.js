import React from 'react';

export default (props) => {
	return (
		<div className="header">
			<h2 className="main-title">Cards & Co.</h2>
			{/* <div className="link-bar settings" onClick={() => props.openModal('settings')}>
				SETTINGS
			</div> */}
			{/* <div className="link-bar rules" onClick={() => props.openModal('rules')}>
				RULES
			</div> */}
			{/* <div className="link-bar about" onClick={() => props.openModal('about')}>
				ABOUT
			</div> */}
		</div>
	);
};
